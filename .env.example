# Server Configuration
NODE_ENV=production
PORT=10000
HOST=0.0.0.0

# MongoDB Connection
# Get this from MongoDB Atlas: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=your_mongodb_connection_string_here

# CORS Configuration
# For production, specify your Flutter app domain
# For testing, use * to allow all origins
CORS_ORIGIN=*

# Firebase Configuration (Optional - for push notifications)
# Path to Firebase service account JSON file
# On hosting platforms, you can upload the file or use environment variables
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Notification Settings
# Set to true to send notifications to online users (for testing)
# Set to false to only send to offline users
SEND_NOTIFICATIONS_TO_ONLINE_USERS=true
